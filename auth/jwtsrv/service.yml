apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: jwtsrv
  labels:
    name: jwtsrv
spec:
  type: LoadBalancer
  ports:
    - port: 8801
      protocol: TCP
      targetPort: 'jwtsrv-port'
  selector:
    name: jwtsrv
